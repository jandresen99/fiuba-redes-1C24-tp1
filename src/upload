import socket
from argparse import ArgumentParser

# UDP_IP = "127.0.0.1"
# UDP_PORT = 6000

# MESSAGE = b"Hola!"

# sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)

# sock.sendto(MESSAGE, (UDP_IP, UDP_PORT))

# print("Message sent:", MESSAGE)

# TODO: crear función build_menu que reciba por parámetro "upload", "download" o "server"
# y te arme el menú
def build_menu():
    parser = ArgumentParser(description='Uploads a file to the server')
    
    group = parser.add_mutually_exclusive_group()
    group.add_argument('-v', '--verbose', action='store_true', help='increase output verbosity')
    group.add_argument('-q', '--quiet', action='store_true', help='decrease output verbosity')

    parser.add_argument('-H', '--host', help='server IP address', metavar='ADDR')
    parser.add_argument('-p', '--port', type=int, help='server port', metavar='PORT')
    parser.add_argument('-s', '--src', help='source file path', metavar='FILEPATH', required=True)
    parser.add_argument('-n', '--name', help='file name', metavar='FILENAME', required=True)
    
    return parser.parse_args()
    

def main():
    args = build_menu()
    
if __name__ == "__main__":
    main()
